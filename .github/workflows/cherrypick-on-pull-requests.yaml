name: Cherry Pick Automation

on:
  pull_request:
    types: [closed]
  workflow_call:

permissions:
  contents: write
  pull-requests: write

jobs:
  cherry-pick:
    if: >
      github.event.pull_request.merged == true &&
      github.event.pull_request.base.ref == 'trunk' &&
      contains(join(fromJson(toJson(github.event.pull_request.labels)).*.name, ','), 'cherry_pick')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Configure Git user
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Extract PR labels
        id: labels
        run: |
          LABELS=$(jq -r '.pull_request.labels[].name' < "$GITHUB_EVENT_PATH")
          echo "labels<<EOF" >> $GITHUB_OUTPUT
          echo "$LABELS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Cherry-pick to target branches
        id: cherry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LABELS: ${{ steps.labels.outputs.labels }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          MERGE_SHA: ${{ github.event.pull_request.merge_commit_sha }}
          REPO: ${{ github.repository }}
        run: |
          # configure git author
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"

          SUCCESS_BRANCHES=()
          FAILED_BRANCHES=()

          # Ensure we iterate lines in the current shell (no subshell)
          IFS=$'<br/>'
          while read -r label; do
            [ -z "$label" ] && continue

            # allow optional space after "CP:"
            if [[ "$label" =~ ^CP:\ ?(.+)$ ]]; then
              target_branch="${BASH_REMATCH[1]}"
              echo "🔍 Found target branch label: $target_branch"

              if git ls-remote --exit-code origin "$target_branch" >/dev/null 2>&1; then
                new_branch="feature/cherry-pick-${PR_NUMBER}-to-${target_branch//\//-}"
                echo "➡️  Creating new branch: $new_branch from $target_branch"

                git fetch origin "$target_branch"
                git checkout -b "$new_branch" "origin/$target_branch"

                if git cherry-pick -x "$MERGE_SHA"; then
                  echo "✅ Cherry-pick successful for $target_branch"
                  git push origin "$new_branch"
                  SUCCESS_BRANCHES+=("$target_branch")

                  # create PR (gh should pick up token from env)
                  gh pr create \
                    --base "$target_branch" \
                    --head "$new_branch" \
                    --title "Cherry pick #${PR_NUMBER} into ${target_branch}" \
                    --body "Automatic cherry-pick of #${PR_NUMBER} into \`${target_branch}\`."
                else
                  echo "⚠️ Cherry-pick failed for $target_branch"
                  FAILED_BRANCHES+=("$target_branch")
                  git cherry-pick --abort || true
                fi
              else
                echo "🚫 Branch $target_branch not found. Skipping."
                FAILED_BRANCHES+=("$target_branch (not found)")
              fi
            fi
          done <<< "$LABELS"

          # Write outputs as multiline values using GITHUB_OUTPUT
          {
            echo "success_branches<<EOF"
            for b in "${SUCCESS_BRANCHES[@]}"; do
              echo "$b"
            done
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          {
            echo "failed_branches<<EOF"
            for b in "${FAILED_BRANCHES[@]}"; do
              echo "$b"
            done
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Comment results on original PR
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          SUCCESS_BRANCHES: ${{ steps.cherry.outputs.success_branches }}
          FAILED_BRANCHES: ${{ steps.cherry.outputs.failed_branches }}
        run: |
          COMMENT="### 🧩 Cherry-pick Summary for #${PR_NUMBER}<br/><br/>"

          # Add successful cherry-picks
          if [ -n "$SUCCESS_BRANCHES" ]; then
            COMMENT+="✅ **Successful cherry-picks:**<br/>"
            while IFS= read -r branch; do
              [ -z "$branch" ] && continue
              COMMENT+="- \`${branch}\`<br/>"
            done <<< "$SUCCESS_BRANCHES"
            COMMENT+="<br/>"
          fi

          # Add failed or skipped cherry-picks
          if [ -n "$FAILED_BRANCHES" ]; then
            COMMENT+="⚠️ **Failed or skipped cherry-picks:**<br/>"
            while IFS= read -r branch; do
              [ -z "$branch" ] && continue
              COMMENT+="- \`${branch}\`<br/>"
            done <<< "$FAILED_BRANCHES"
            COMMENT+="<br/>"
          fi

          # If no results found
          if [ -z "$SUCCESS_BRANCHES" ] && [ -z "$FAILED_BRANCHES" ]; then
            COMMENT+="No valid \`CP:\` labels found. Nothing to cherry-pick.<br/>"
          fi

          # Output for debugging/log
          echo -e "$COMMENT"

          # Post comment on the original PR
          gh pr comment "$PR_NUMBER" --body "$COMMENT"
