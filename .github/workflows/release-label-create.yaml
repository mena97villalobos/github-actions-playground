name: Create CP Label on Release Branch

on:
  create:
    branches:
      - 'release*' # triggers when any release* branch is created

jobs:
  create-label:
    runs-on: ubuntu-latest

    permissions:
      issues: write
      contents: read

    steps:
      - name: Extract branch name
        id: branch
        run: |
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          echo "name=$BRANCH_NAME" >> $GITHUB_OUTPUT

      - name: Create Label
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const branchName = "${{ steps.branch.outputs.name }}";
            const labelName = `CP: ${branchName}`;
            const description = "Label for automatic cherry picking branch ${branchName}";

            try {
              await github.rest.issues.createLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: labelName,
                color: "ff0000", // red
                description: description
              });
              console.log(`Label created: ${labelName}`);
            } catch (err) {
              if (err.status === 422) {
                console.log(`Label already exists: ${labelName}`);
              } else {
                throw err;
              }
            }
